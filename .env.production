# REVIEW: Committing `.env.production` couples the repo to one deployment URL; many teams gitignore env files and set `VITE_API_URL` only in CI (GitHub `vars`) to avoid drift between forks and environments.
VITE_API_URL=https://react-javascript-api.onrender.com
